# ✅ Task Tracker CLI

أداة سطر أوامر (CLI) لإدارة المهام اليومية (To-Do List) باستخدام Node.js وملف JSON بسيط. تدعم الإضافة، التحديث، الحذف، التصفية حسب الحالة، وتغيير حالة المهمة.

## 🚀 الميزات

- ➕ إضافة مهمة جديدة
- 📝 تحديث وصف مهمة
- ❌ حذف مهمة حسب ID
- 📋 عرض المهام حسب الحالة (todo, in-progress, done)
- 🔁 تغيير حالة المهمة إلى "in-progress" أو "done"
- 💾 تخزين المهام في ملف `tasks.json`

## 🛠️ المتطلبات

- Node.js >= 18
- لا حاجة لأي مكتبات خارجية

## 📦 التثبيت

```bash
npm install
```

## ▶️ الاستخدام

```bash
node index.js task-cli <command> [options]
```

## 🧪 أوامر مدعومة

### 📋 عرض المهام

عرض كل المهام:

```bash
node index.js task-cli list
```

عرض المهام المكتملة:

```bash
node index.js task-cli list done
```

عرض المهام قيد التنفيذ:

```bash
node index.js task-cli list in-progress
```

عرض المهام التي لم تبدأ بعد:

```bash
node index.js task-cli list todo
```
### ➕ إضافة مهمة

```bash
node index.js task-cli add "Buy groceries"
```
### 📝 تحديث وصف مهمة

```bash
node index.js task-cli update 1 "Buy groceries and cook dinner"
```
### ❌ حذف مهمة

```bash
node index.js task-cli delete 1
```
### 🔁 تغيير حالة المهمة

إلى "in-progress":

```bash
node index.js task-cli mark-in-progress 1
```

إلى "done":

```bash
node index.js task-cli mark-done 1
```
## 📂 هيكل المشروع

```
├── index.js           # الكود الرئيسي للأداة
├── tasks.json         # ملف تخزين المهام
└── README.md
```
## 🧠 ملاحظات

- يتم إنشاء ملف `tasks.json` تلقائيًا إذا لم يكن موجودًا.
- يتم توليد ID تلقائيًا بزيادة آخر ID موجود.
- يتم تسجيل وقت الإنشاء والتحديث لكل مهمة.
